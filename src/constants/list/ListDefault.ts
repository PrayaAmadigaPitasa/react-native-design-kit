import {ListActivity, ListFilter} from '../../components';

export const LIST_ACTIVITY_DEFAULT: ListActivity = {
  loading: true,
  endPage: false,
};

export const LIST_FILTER_DEFAULT: ListFilter = {
  page: 1,
};

export const LIST_END_THRESHOLD_DEFAULT = 300;
export const LIST_HORIZONTAL_SCROLL_INDICATOR_DEFAULT = false;
